(()=>{function isScoreInput(el){return el&&el.matches&&el.matches('#scoresBox input[type="number"], #scoresBox input.score-input, #scoresBox .score-input input[type="number"]')}function stepInput(el,dir){const step=Number(el.step||1)||1,min=el.min===''?-Infinity:Number(el.min),max=el.max===''?Infinity:Number(el.max),cur=Number(el.value||0)||0;let next=cur+(dir<0?step:-step);next=Math.max(min,Math.min(max,next));el.value=String(next);el.dispatchEvent(new Event('input',{bubbles:true}));el.dispatchEvent(new Event('change',{bubbles:true}))}function bind(){document.addEventListener('wheel',e=>{const el=e.target;if(!isScoreInput(el))return;if(document.activeElement!==el)el.focus({preventScroll:true});e.preventDefault();stepInput(el,e.deltaY)},{passive:false});document.addEventListener('focusin',e=>{if(isScoreInput(e.target))e.target.classList.add('scores-wheel-focus')});document.addEventListener('focusout',e=>{if(isScoreInput(e.target))e.target.classList.remove('scores-wheel-focus')})}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',bind);else bind();})();