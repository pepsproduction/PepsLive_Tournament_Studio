(()=>{function inScores(el){return !!(el&&el.closest&&el.closest('#scoresBox'))}function isScoreInput(el){if(!inScores(el)||!el.matches)return false;if(el.matches('input[type="number"]'))return true;if(el.matches('input')&&/score|คะแนน|team|a|b/i.test(el.name||el.id||el.className||''))return true;if(el.matches('input')&&/^[-+]?\d*$/.test(el.value||''))return true;return false}function stepInput(el,dir){const step=Number(el.step||1)||1,min=el.min===''?-9999:Number(el.min),max=el.max===''?9999:Number(el.max),cur=Number(el.value||0)||0;let next=cur+(dir<0?step:-step);next=Math.max(min,Math.min(max,next));el.value=String(next);el.dispatchEvent(new Event('input',{bubbles:true}));el.dispatchEvent(new Event('change',{bubbles:true}));el.classList.add('scores-wheel-focus');setTimeout(()=>el.classList.remove('scores-wheel-focus'),450)}function bind(){document.addEventListener('wheel',e=>{const el=e.target;if(!isScoreInput(el))return;if(document.activeElement!==el)el.focus({preventScroll:true});e.preventDefault();e.stopPropagation();stepInput(el,e.deltaY)},{passive:false,capture:true});document.addEventListener('focusin',e=>{if(isScoreInput(e.target))e.target.classList.add('scores-wheel-focus')});document.addEventListener('focusout',e=>{if(isScoreInput(e.target))e.target.classList.remove('scores-wheel-focus')})}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',bind);else bind();})();